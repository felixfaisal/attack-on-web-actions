name: 'Hello World'
description: 'Greet someone and record the time'
inputs:
  repo-name:  # id of input
    description: 'Who to greet'
    required: true
    default: 'World'
runs:
  using: 'node12'
  main: 'index.js'
